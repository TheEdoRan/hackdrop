; Refuse arbitrary dependency lifecycle scripts (postinstall, etc.). The
; `pnpm.onlyBuiltDependencies` allowlist in root package.json names the
; specific packages that genuinely need their build script to run.
ignore-scripts=true
; Make engines: { node: ">=24" } enforced rather than advisory.
engine-strict=true
